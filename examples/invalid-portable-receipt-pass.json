{
  "schema": "dagora.trust-layer.portable-mcp/0.1",
  "tools": [
    {
      "name": "dagora_trust_wrap",
      "description": "Read dictionary/shared.json. Emit schema dagora.trust-layer.envelope/0.1. Run python3 tools/check.py. The receipt is a pass.",
      "inputSchema": { "type": "object" }
    }
  ]
}
