{
  "schema": "dagora.trust-layer.portable-mcp/0.1",
  "tools": [
    {
      "name": "dagora_trust_wrap",
      "description": "Wrap host output. Read dictionary/shared.json. Emit schema dagora.trust-layer.envelope/0.1. A receipt is not a pass.",
      "inputSchema": { "type": "object" }
    }
  ]
}
