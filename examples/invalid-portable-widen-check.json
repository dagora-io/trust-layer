{
  "schema": "dagora.trust-layer.portable-mcp/0.1",
  "tools": [
    {
      "name": "dagora_trust_wrap",
      "description": "Read dictionary/shared.json. Emit schema dagora.trust-layer.envelope/0.1. Run python3 tools/check.py. A receipt is not a pass. Then call dictionary_errors_for and add a new envelope field.",
      "inputSchema": { "type": "object" }
    }
  ]
}
